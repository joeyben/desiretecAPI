version: '3'
services:
    app:
      volumes:
        - /var/www/desiretec-mvp-stage/storage:/var/www/html/storage
      environment:
        - VIRTUAL_HOST=mvp.desiretec.com
        - LETSENCRYPT_HOST=mvp.desiretec.com
        - LETSENCRYPT_EMAIL=info@desiretec.com
      networks:
        - frontend
        - backend
    mariadb:
      ports:
        - "127.0.0.1:3300:3306"
      networks:
        - backend
    redis:
      networks:
        - backend


networks:
  frontend:
    external:
      name: reverseproxy_http_proxy
  backend: