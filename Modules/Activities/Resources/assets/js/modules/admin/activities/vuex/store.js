import * as actions from './actions'
import * as getters from './getters'

const state = {
    activities: []
}

const mutations = {
}

export default {
    state,
    getters,
    actions,
    mutations
}
